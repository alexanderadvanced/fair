{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row">
        <div class="col-sm-2">
            <button class="btn btn-primary">{{ button_label|default('Save')|trans }}</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}